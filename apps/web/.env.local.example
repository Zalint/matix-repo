# Mode auth — 'dev' (header X-Dev-Tenant-Id) ou 'keycloak' (NextAuth + JWT)
NEXT_PUBLIC_AUTH_MODE=keycloak

# API
NEXT_PUBLIC_API_URL=http://localhost:3001

# NextAuth
AUTH_SECRET=local-dev-only-secret-replace-me
NEXTAUTH_URL=http://localhost:3000

# Keycloak (server-side, NE PAS préfixer NEXT_PUBLIC)
KEYCLOAK_CLIENT_ID=matix-web
KEYCLOAK_CLIENT_SECRET=WX3EwSVA1wxGySWGga6LrBAOFWRnDbfm
KEYCLOAK_ISSUER=http://localhost:8180/realms/matix
